language: go
sudo: required
go:
  - 1.4
  - tip

before_install:
  - export PATH=$PATH:$TRAVIS_BUILD_DIR

install:
  - go get github.com/stretchr/testify
  - export PATH=$PATH:$TRAVIS_BUILD_DIR

notifications:
  email:
    - rakzrao81@gmail.com

script: ls -ltr /home/travis/gopath/src/github.com/raks81/go-acousticid/fpcalc && ls -ltr /home/travis/gopath/src/github.com/raks81/go-acousticid && /home/travis/gopath/src/github.com/raks81/go-acousticid/fpcalc test.mp3 && go test
